/**
 * OpenAPI-derived paths/components types (openapi-typescript output).
 * This file lives under `src/lib/` so codegen does not overwrite it — see CONTRIBUTING.md.
 */
export type { paths, components, operations, webhooks } from '../openapi/openapi-types';
